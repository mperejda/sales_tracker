.container
  .page-header
    = link_to leads_path, :class => 'btn btn-default' do
      %span.glyphicon.glyphicon-list-alt
      Back
    = link_to @lead, :class => 'btn btn-primary' do
      %span.glyphicon.glyphicon-info-sign
      Show
    %h1 Editing lead

  = render 'form'
